apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-user
  namespace: udagram
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-user
  template:
    metadata:
      labels:
        app: api-user
    spec:
      containers:
      - name: api-user
        image: kaminodev/udagram-api-user:latest
        env:
        - name: POSTGRES_HOST
          valueFrom:
            configMapKeyRef: { name: app-config, key: POSTGRES_HOST }
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef: { name: app-config, key: POSTGRES_DB }
        - name: POSTGRES_USERNAME
          valueFrom:
            secretKeyRef:   { name: app-secrets, key: POSTGRES_USERNAME }
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:   { name: app-secrets, key: POSTGRES_PASSWORD }
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:   { name: app-secrets, key: JWT_SECRET }
        ports:
        - containerPort: 8080
        readinessProbe: { tcpSocket: { port: 8080 }, initialDelaySeconds: 10, periodSeconds: 10 }
        livenessProbe:  { tcpSocket: { port: 8080 }, initialDelaySeconds: 20, periodSeconds: 20 }
---
apiVersion: v1
kind: Service
metadata:
  name: api-user
  namespace: udagram
spec:
  type: ClusterIP
  selector:
    app: api-user
  ports:
  - port: 8080
    targetPort: 8080
